import { IResolvers } from "@graphql-tools/utils";
import FedExService from "../../services/fedex.service";

const fedexResolversMutation: IResolvers = {
  Mutation: {

  },
};

export default fedexResolversMutation;
