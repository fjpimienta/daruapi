"Tipo de objeto para almacenar la informacion de los Modelos de los Productos"
type Model implements Item {
  id: ID!
  description: String!
  slug: String!
  order: Int!
  active: Boolean
  suppliersCat: [SupplierCat]
}

type SupplierCat {
  idProveedor: ID
  name: String
  slug: String
}
