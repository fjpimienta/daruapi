"Tipo de objeto para almacenar la informacion de las Marcas de los Productos"
type Catalog implements Item {
  id: ID!
  description: String!
  slug: String!
  active: Boolean
 suppliersCat: [SupplierCat]
}

type SupplierCat {
  idProveedor: ID
  name: String
  slug: String
}