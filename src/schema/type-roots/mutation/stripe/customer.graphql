"Definiciones de los tipos de raíz"
type Mutation {
   "Clientes"
   createCustomer(email:String!, name:String!): ResultStripeCustomer
   updateCustomer(id: ID!, customer: CustomerInput!): ResultStripeCustomer
   deleteCustomer(id: ID!): ResultStripeCustomer
}