input ChargeInput {
   token: String
   "Cantidad a pagar en formato string, para convertirlo a entero cero decimal"
   amount: String!
   "Resumen del pedido"
   description: String!
   "Cliente (ID)"
   customer: ID!
   "Divisa"
   currency: CurrencyEnum = MXN
}