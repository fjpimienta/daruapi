// import BBVAService from "../../services/bbva.service";

// const bbvaResolvers = {
//   Mutation: {
//     createPayment: async (_source: any, { input }: { input: any }, { dataSources }: { dataSources: { bbvaService: BBVAService } }) => {
//       return await dataSources.bbvaService.createPayment(input);
//     },
//     cancelPayment: async (_source: any, { id }: { id: string }, { dataSources }: { dataSources: { bbvaService: BBVAService } }) => {
//       return await dataSources.bbvaService.cancelPayment(id);
//     },
//   },
// };

// export default bbvaResolvers;
